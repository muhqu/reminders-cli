// The CLI version reported by `reminders --version`.
//
// Default for local/dev builds; the release workflow
// (.github/workflows/release.yml) overwrites this file with the resolved
// release version before building the published artifact.
let remindersVersion = "0.0.0-dev"
